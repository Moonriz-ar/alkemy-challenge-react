import Button from "react-bootstrap/Button";

const MenuButton = () => {
  return (
    <Button variant="danger" className="position-button">
      <p>See menu</p>
    </Button>
  );
};

export default MenuButton;
